# MongoDB container bootstrap credentials for local development only.
# Copy this file to .env.compose, choose local credentials, and run:
# docker compose --env-file .env.compose up -d mongodb
MONGO_ROOT_USERNAME=replace-with-local-mongodb-user
MONGO_ROOT_PASSWORD=replace-with-local-mongodb-password
